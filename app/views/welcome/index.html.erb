<CENTER>
  <h1>WELCOME TO CHAT APP </h1>
<!--   <%= link_to 'GO',controller: 'posts' %> -->
</CENTER>

<%- if user_signed_in? %>
  <li><%= link_to 'My Posts',posts_path %></li>
<% else %>
  
<% end %>
 
<table class="table">
  <thead>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th></th>
  </tr>
  </thead>
 <tbody>
  <% @post.each do |post| %>
  
        <tr>
          <td><%= post.title %></td>
          <td><%= post.text %></td>
          <td><%= link_to 'show', post_path(post), class: "btn btn-danger" %></td>

          <% end %>
      </tr>


 
<center> <%= link_to 'New Posts',new_post_path %> </center>
 </tbody>
</table>

